#include "main.h"
#include <stdio.h>
/**
* _puts - Print string to stout
* @str: Is a string
* Description: Prints string
* Return: returns (0)
**/
void _puts(char *str)
{
puts(str);
puts("\n");
return (0);
}
