#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _strlen - Length of a string
* @str: Is a string in double quotes
* Description: Prints string
* Return: returns (0)
**/
void _puts(char *str)
{
puts(str);
puts("\n");
return (0);
}
